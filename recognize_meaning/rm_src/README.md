# Обработка отзывов о школах с использованием ИИ

## 📋 Содержание
1. [Общее описание](#общее-описание)
2. [Установка и требования](#установка-и-требования)
3. [Использование](#использование)
4. [Объяснение тем](#объяснение-тем)
5. [Проблемы и решения](#проблемы-и-решения)
6. [Технические детали](#технические-детали)

---

## Общее описание

Этот скрипт обрабатывает отзывы о школах с использованием ИИ-модели для определения тем и тональности. Используется модель `ai-forever/ruBert-base` (приоритет) или `cointegrated/rubert-tiny2` для zero-shot классификации.

**Особенности:**
- Использование более мощной модели для лучшей точности
- RAG (Retrieval-Augmented Generation) с few-shot examples в hypothesis_template
- Оптимизированный порог для обнаружения тем (0.4) - находит несколько тем
- Дополнительная проверка релевантности тем через ключевые слова
- Fallback метод на основе правил (если модель недоступна)
- Поддержка rating для корректировки результатов
- Находит до 5 релевантных тем в одном отзыве

---

## Установка и требования

### Требования
- Python 3.8+
- pip install transformers torch sentencepiece
- ~3-4 GB свободного места на диске
- ~500-700 MB оперативной памяти
- Интернет для первой загрузки модели

### Установка
```bash
pip install transformers torch sentencepiece
```

---

## Использование

### Базовый запуск
```bash
python rm_main_ai.py
```

### Принудительная перезагрузка модели
```bash
python rm_main_ai.py --reload
```

### Очистка кеша модели
```bash
python clear_model_cache.py
```

---

## Объяснение тем

### Тема "администрация"

**Что оценивает:**
- Работу директора школы
- Работу завучей
- Работу административного персонала
- Общее управление школой
- Решения руководства

**Примеры упоминаний:**
- "Директор отличный" → администрация: pos
- "Завуч не помогает" → администрация: neg
- "Администрация работает хорошо" → администрация: pos
- "Руководство не реагирует на жалобы" → администрация: neg

**Ключевые слова:**
- директор, завуч, администрация, руководство, управление

### Все темы

1. **учителя** - качество преподавания, отношение учителей к ученикам
2. **еда** - качество питания в столовой
3. **администрация** - работа директора, завучей, руководства
4. **буллинг** - травля, конфликты между учениками
5. **инфраструктура** - спортзал, стадион, кабинеты, оборудование
6. **охрана** - безопасность, пропускной режим
7. **уборка** - чистота в школе
8. **ремонт** - состояние здания, ремонтные работы
9. **питание** - синоним "еда"
10. **атмосфера** - общая обстановка в школе
11. **досуг** - кружки, секции, внеурочная деятельность
12. **безопасность** - синоним "охрана"
13. **расписание** - организация учебного процесса

---

## Проблемы и решения

### Проблема: Модель не загружается

**Ошибка:** `cointegrated/rubert-base is not a valid model identifier`

**Решение:** Модель `rubert-base` не существует. Используется `rubert-tiny2` или `rubert-tiny`. Код автоматически пробует загрузить доступные модели.

### Проблема: Код обрабатывает все за секунду

**Причина:** Используется fallback метод вместо ИИ-модели.

**Решение:**
1. Очистить кеш: `python clear_model_cache.py`
2. Запустить с перезагрузкой: `python rm_main_ai.py --reload`
3. Проверить вывод - должно быть: `[OK] Модель загружена успешно!`

### Проблема: Отзывы обрабатываются неправильно

**Пример:** Отзыв "Учителя плохие" не находит тему "учителя"

**Решение:** 
- Fallback логика должна находить тему по ключевым словам
- Проверьте отладочные сообщения: `[DEBUG] Fallback: добавлена тема...`
- Если fallback не срабатывает, проверьте список ключевых слов в коде

### Проблема: Модель находит несуществующие темы

**Решение:**
- Повышен порог обнаружения тем до 0.55 (было 0.3)
- Добавлена проверка ключевых слов для каждой темы
- Для некоторых тем требуется явное упоминание

---

## Технические детали

### Используемая модель

**Модель:** `cointegrated/rubert-tiny2` или `cointegrated/rubert-tiny`

**Почему не base?** Модель `cointegrated/rubert-base` не существует на Hugging Face. Используются доступные версии tiny.

### Fallback метод

Если ИИ-модель недоступна, используется метод на основе правил:
- Поиск тем по ключевым словам
- Определение тональности на основе rating
- Простой анализ текста

### Ключевые слова для fallback

**Тема "учителя":**
- Основные формы: учитель, учителя, учителей, учителю, учителем
- Преподаватели: преподаватель, преподаватели, педагог, педагоги
- Контекстные фразы: "плохое отношение учителей", "учителя плохие", "учителя хорошие", "учителя начальной школы"
- Отдельные слова: злые, ненавистные, учат, обучают

### Порог обнаружения тем

**Текущий порог:** 0.4 (оптимизирован для нахождения нескольких тем)

**Почему:** Пониженный порог позволяет находить больше релевантных тем в одном отзыве. Ограничение максимум 5 темами предотвращает перегрузку.

### Очистка кеша

**Автоматическая:**
```bash
python clear_model_cache.py
```

**Ручная (Windows):**
```bash
rmdir /s "%USERPROFILE%\.cache\huggingface\hub\models--cointegrated"
```

**Ручная (Linux/Mac):**
```bash
rm -rf ~/.cache/huggingface/hub/models--cointegrated
```

---

## Формат выходных данных

```json
{
  "review_id": "1",
  "school_id": "2",
  "text": "Отличная школа! Еда вкусная, учителя добрые",
  "rating": 5,
  "topics": {
    "еда": "pos",
    "учителя": "pos"
  },
  "overall": "pos"
}
```

**Поля:**
- `topics` - словарь тем и их тональности (pos/neg)
- `overall` - общая тональность отзыва (pos/neg)
- `main_idea` и `tonality` - **убраны** (дубликаты информации)

---

## Безопасность библиотек

Все используемые библиотеки из официальных источников:
- `transformers` - официальная библиотека от Hugging Face
- `torch` (PyTorch) - официальный фреймворк от Meta/Facebook
- `sentencepiece` - библиотека от Google

**Безопасны для установки.**

---

## Производительность

**С ИИ-моделью:**
- ~1-3 секунды на отзыв (CPU)
- ~0.1-0.5 секунды на отзыв (GPU)
- 1000 отзывов: 16-50 минут (CPU) или 2-8 минут (GPU)

**С fallback методом:**
- ~0.001 секунды на отзыв
- 1000 отзывов: ~1 секунда

---

## Отладка

### Включение отладочного режима

В файле `rm_main_ai.py` найдите:
```python
DEBUG_MODE = False  # Установите True для отладки
```

Измените на:
```python
DEBUG_MODE = True
```

Это выведет информацию о найденных ключевых словах и добавленных темах.

### Проверка загруженной модели

После запуска должно быть:
```
[OK] Модель загружена успешно! Модель: cointegrated/rubert-tiny2
```

Если видите `[WARN] ВНИМАНИЕ: Модель не загружена` - модель не используется, работает fallback.

