# Визуализация школ и отзывов

Простое React приложение для визуализации данных о школах и отзывах.

## Установка

```bash
npm install
```

## Запуск

```bash
npm start
```

Приложение откроется на http://localhost:3000

## Настройка API

По умолчанию приложение подключается к API на http://localhost:8000

Чтобы изменить URL API, создайте файл `.env` в корне проекта `UI`:

```
REACT_APP_API_URL=http://your-api-url:8000
```

## Функционал

- Выбор школы из выпадающего списка
- Выбор топика (ремонт, учителя, еда, администрация, буллинг, инфраструктура, охрана, уборка)
- Фильтрация по датам (от и до)
- Визуализация динамики упоминаний топиков во времени в виде линейной диаграммы

## Структура

- `src/App.js` - основной компонент приложения
- `src/components/Filters.js` - компонент фильтров
- `src/components/Chart.js` - компонент графика

## Тест: геокод для МОУ «СОШ № 6»

При выборе школы **№6** по названию в 2ГИС или Яндексе — например **МОУ «СОШ № 6»** или **МОУ Средняя общеобразовательная школа №6** (не путать с №66 и т.п.) — на карте дополнительно показываются:

- оранжевые круги — координаты домов из `src/data/geocoded_addresses_sosh6.json`;
- синие полупрозрачные полигоны — улицы из `src/data/street_polygons_sosh6.json`.

Исходные файлы лежат в `geocoding/geocode_test/`. После обновления геокода скопируйте их в `UI/src/data/` (имена с суффиксом `_sosh6`) и пересоберите приложение.

