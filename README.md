# Анализ образовательных учреждений Саратова

Комплексная система для сбора, обработки и анализа данных об образовательных учреждениях города Саратова из множественных источников с применением методов машинного обучения и NLP.

## 🎯 Цель проекта

Создание единой аналитической платформы для комплексной оценки школ на основе:
- **Инфраструктурных данных**: год постройки, площадь, этажность, наличие спортивных объектов
- **Пользовательских отзывов**: анализ тональности и тематики отзывов из Яндекс.Карт и 2ГИС
- **Рейтинговых показателей**: агрегированные оценки из различных источников
- **Географических данных**: координаты, кадастровые номера, близлежащие адреса

## 🏗️ Архитектура проекта

Проект построен по модульному принципу с четким разделением данных и кода:

```
analysis-of-educational-institutions/
├── parsing/                    # Модуль парсинга данных
│   ├── 2gis/                   # Парсинг школ и отзывов из 2ГИС
│   ├── yandex_maps/            # Парсинг школ и отзывов из Яндекс.Карт
│   ├── google_maps/            # Парсинг школ из Google Maps
│   ├── uchi_ru/                # Парсинг школ с сайта Uchi.ru
│   ├── find_cadastral_number/ # Поиск кадастровых номеров
│   ├── vectorization_and_merging/ # Векторизация и объединение данных
│   └── parsing_data/           # Финальное объединение данных
├── recognize_meaning/            # Анализ смысла и тональности отзывов
├── geocoding/                  # Геокодирование адресов
├── adreses_nears_school/       # Анализ адресов рядом со школами
├── final_analys/               # Финальный анализ и кластеризация
├── visualization/               # Визуализация данных на карте
├── db/                         # Работа с базой данных PostgreSQL
├── api/                        # REST API на FastAPI
└── UI/                         # React приложение для визуализации
```

## 📊 Пайплайн обработки данных

### Этап 1: Парсинг данных из источников

#### 1.1 Парсинг школ (2ГИС, Яндекс.Карты, Google Maps, Uchi.ru)

**Особенности реализации:**
- **Многоэтапный парсинг**: сначала собирается список школ, затем детальная информация
- **Обход защиты от ботов**: использование Selenium с настройками для имитации реального пользователя
- **Обработка капчи**: интерактивная пауза для ручного ввода капчи при необходимости
- **Извлечение данных**: название, адрес, рейтинг, количество отзывов, координаты

**Результат:** JSON файлы с данными о школах из каждого источника

#### 1.2 Парсинг отзывов

**Особенности:**
- Парсинг всех отзывов для каждой школы
- Извлечение: текст, дата, рейтинг, количество лайков/дизлайков
- Сохранение HTML для отладки и повторного анализа

**Результат:** JSON файлы с отзывами по каждой школе

#### 1.3 Поиск кадастровых номеров

Автоматический поиск кадастровых номеров зданий школ на сайте кадастр.сайт для связи с данными о недвижимости.

### Этап 2: Объединение данных из разных источников

#### 2.1 Векторизация и сопоставление школ

**Метод:** Использование модели `sentence-transformers/all-mpnet-base-v2` для векторизации названий и адресов школ.

**Алгоритм:**
1. Подготовка текста: объединение названия и адреса школы
2. Векторизация: преобразование в 768-мерные векторы
3. Поиск совпадений: вычисление косинусного сходства между векторами
4. Объединение: создание единых записей для школ с высоким сходством (>0.78)

**Преимущества:**
- Автоматическое сопоставление школ с разными названиями в разных источниках
- Учет вариаций в написании адресов
- Объединение данных из разных источников в единую структуру

#### 2.2 Нормализация и финальное объединение

**Процесс:**
1. Нормализация названий: удаление МОУ/МАОУ/ГОУ и т.д.
2. Нормализация адресов: удаление "Саратов", индексов, приведение к единому формату
3. Объединение по адресу или названию
4. Создание единого JSON файла со всеми школами

**Результат:** `gold_school_data.json` - единая база данных всех школ

### Этап 3: Геокодирование

Преобразование адресов школ в координаты (широта/долгота) с использованием API Яндекс.Карт для последующей визуализации и пространственного анализа.

### Этап 4: Анализ отзывов (Recognize Meaning)

#### 4.1 Извлечение тем

**8 категорий тем:**
- Ремонт (стены, туалеты, окна, отопление)
- Учителя (педагоги, уроки, обучение)
- Еда (столовая, питание, меню)
- Администрация (директор, управление)
- Буллинг (травля, конфликты)
- Инфраструктура (спортзал, бассейн, стадион)
- Охрана (безопасность, пропуск)
- Уборка (чистота, санитария)

**Метод:** Поиск ключевых слов с учетом падежей и множественного числа в русском языке.

#### 4.2 Определение тональности

**Трехуровневая система:**
- Позитивная (pos)
- Негативная (neg)
- Нейтральная (neu)

**Алгоритм:**
1. Использование рейтинга (если доступен): 4-5 = позитивная, 1-2 = негативная
2. Анализ ключевых слов: поиск позитивных/негативных индикаторов
3. Анализ фраз: поиск конструкций типа "не рекомендую"
4. Определение общей тональности отзыва

**Выходной формат:**
```json
{
  "topics": {
    "учителя": "pos",
    "еда": "neg",
    "ремонт": "pos"
  },
  "overall": "pos"
}
```

#### 4.3 Агрегация по школам

Для каждой школы вычисляются метрики:
- Количество отзывов по каждой теме
- Доля негативных отзывов по теме
- Средняя тональность по теме (-1 до +1)
- Динамика негативных отзывов за последние 12 месяцев

### Этап 5: Работа с инфраструктурой

Объединение данных о школах с информацией о зданиях:
- Год постройки и реконструкции
- Площадь и этажность
- Наличие спортивных объектов (бассейн, стадион, ФОК)
- Расчет инфраструктурных скоров (modernity_score, scale_score, amenities_score)

### Этап 6: Анализ адресов рядом со школами

**Цель:** Оценка влияния школы на стоимость недвижимости и инфраструктуру района.

**Процесс:**
1. Определение адресов в радиусе от школы
2. Связывание с данными о недвижимости
3. Анализ корреляций между характеристиками школы и стоимостью квартир

### Этап 7: Финальный анализ и кластеризация

**Анализ включает:**
- Кластеризацию школ по схожести инфраструктуры
- Временной анализ отзывов (динамика по месяцам)
- Корреляционный анализ между различными метриками
- Построение рейтингов школ по различным критериям

**Кластеры школ:**
- Крепкие середняки
- Новые и крупные комплексы
- Старый фонд с базовой инфраструктурой
- Школы с уникальным оснащением (бассейны, стадионы)

### Этап 8: Хранение и API

**База данных PostgreSQL:**
- Схема `ca` с таблицами `school` и `review`
- Нормализованная структура для эффективных запросов
- Поддержка фильтрации по датам и школам

**REST API (FastAPI):**
- `GET /schools` - список всех школ
- `GET /schools/{school_id}/reviews` - отзывы по школе с фильтрацией по датам
- Автоматическая документация (Swagger UI, ReDoc)

### Этап 9: Визуализация

**React приложение:**
- Интерактивная карта со школами
- Фильтрация по темам отзывов
- Визуализация динамики упоминаний тем во времени
- Графики и диаграммы для анализа

## 🔧 Технологический стек

### Backend
- **Python 3.7+**
- **Selenium** - автоматизация браузера для парсинга
- **BeautifulSoup4** - парсинг HTML
- **sentence-transformers** - векторизация текста для объединения данных
- **scikit-learn** - вычисление косинусного сходства
- **FastAPI** - REST API
- **PostgreSQL** - база данных
- **psycopg2** - драйвер PostgreSQL

### Frontend
- **React** - пользовательский интерфейс
- **Folium** - генерация интерактивных карт

### Data Processing
- **pandas** - обработка данных
- **numpy** - численные вычисления
- **matplotlib/seaborn** - визуализация

## 📈 Ключевые особенности проекта

### 1. Умное объединение данных
- Использование векторных представлений для сопоставления школ с разными названиями
- Автоматическое разрешение неоднозначностей
- Сохранение всех источников данных для трассируемости

### 2. Глубокий анализ отзывов
- Извлечение 8 тематических категорий
- Определение тональности без использования платных API
- Учет временной динамики отзывов
- Агрегация метрик на уровне школы

### 3. Комплексная оценка школ
- Инфраструктурные показатели
- Пользовательские оценки
- Пространственный анализ
- Временные тренды

### 4. Масштабируемая архитектура
- Модульная структура
- Разделение данных и кода
- REST API для интеграции
- Готовность к расширению

## 🚀 Быстрый старт

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# База данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database
DB_USER=your_user
DB_PASSWORD=your_password

# API ключи
YANDEX_API_KEY=your_yandex_api_key
```

### Запуск API

**Важно:** команды запуска нужно выполнять **из корня репозитория** (`analysis-of-educational-institutions/`), а не из папки `api/`. Модуль приложения называется `api.main`, а не `app.main`.

Активируйте виртуальное окружение (если используете venv) и запустите сервер:

```bash
# Из корня проекта:
./venv/Scripts/activate          # Windows
# source venv/bin/activate       # Linux/macOS

uv run uvicorn api.main:app --reload
# или без uv:
# uvicorn api.main:app --reload
```

API будет доступно по адресу: **http://127.0.0.1:8000**

Если появляется ошибка `ModuleNotFoundError: No module named 'app'` — вы, скорее всего, запустили команду из папки `api/` или указали неверный модуль. Нужно: выйти в корень проекта (`cd ..`) и запускать `api.main:app` (именно **api**, не app).

### Запуск UI

Из корня репозитория перейдите в папку фронта **именно как `UI`** (с большой буквы), иначе на Windows может возникнуть конфликт ESLint («Plugin "react" was conflicted between ui\\node_modules and UI\\node_modules»):

```bash
cd UI
npm install
npm start
```

Фронтенд откроется на **http://localhost:3000** и по умолчанию обращается к API по адресу `http://localhost:8000`. Чтобы задать другой URL API, создайте в `UI/` файл `.env` с переменной `REACT_APP_API_URL=http://127.0.0.1:8000`.

Если при сборке появляется ошибка ESLint *«Plugin "react" was conflicted between ... ui\\node_modules ... and ... UI\\node_modules»*: в папке `UI` создайте файл `.env` с одной строкой `DISABLE_ESLINT_PLUGIN=true`, затем перезапустите `npm start`. Так ESLint при сборке отключается и конфликт путей на Windows больше не мешает.

## 📝 Примеры использования

### Парсинг школ из 2ГИС

```bash
# 1-й этап: список школ
python parsing/2gis/tgis_src/2gis_school_parser/2gis_1_stage_school_parser.py

# 2-й этап: детальная информация
python parsing/2gis/tgis_src/2gis_school_parser/2gis_2_stage_school_parser.py

# Парсинг отзывов
python parsing/2gis/tgis_src/2gis_review_parser/2gis_review_parser.py
```

### Объединение данных

```bash
# Векторизация и объединение
python parsing/vectorization_and_merging/vam_src/vectorization_and_merging.py

# Финальное объединение
python parsing/parsing_data/pd_src/parsing_data_main.py
```

### Анализ отзывов

```bash
python recognize_meaning/rm_src/rm_main.py
```

### Создание базы данных

```bash
# Создание схемы и таблиц
python db/db_src/db_create/create_object.py

# Вставка данных о школах
python db/db_src/db_insert/insert_data_school.py

# Вставка отзывов
python db/db_src/db_insert/insert_data_review.py
```

## 📚 Документация

Каждый модуль имеет собственный README с детальным описанием:
- `parsing/README.md` - описание модуля парсинга
- `recognize_meaning/README.md` - описание анализа отзывов
- `db/README.md` - описание работы с БД
- `api/README.md` - описание API
- `UI/README.md` - описание пользовательского интерфейса

## 🎓 Применение

Проект может быть использован для:
- **Аналитики образования**: комплексная оценка школ города
- **Исследований**: анализ влияния инфраструктуры на качество образования
- **Недвижимости**: оценка влияния школ на стоимость жилья
- **Городского планирования**: оптимизация размещения образовательных учреждений

## 📄 Лицензия

Проект для образовательных целей.

## 👤 Автор

Проект разработан для анализа образовательных учреждений города Саратова.
